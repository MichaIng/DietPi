#!/bin/dash
echo 'Removing obsolete initramfs images'
for f in $(find /boot -maxdepth 1 -name 'initrd.img-*' -o -name 'uInitrd-*')
do
	[ ! -d "/lib/modules/${f#*-}" ] || continue
	rm -v "$f"
done
exit 0
